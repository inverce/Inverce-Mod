<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.inverce.mod.core">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- This allows user to not think about initializing our MOD -->
        <provider
            android:name=".internal.initialization.InitializationContentProvider"
            android:authorities="${applicationId}.im.initialization.provider"
            android:exported="false" />
    </application>
</manifest>
